<Window x:Class="WpeAgent.ReferenceUiWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="WPE // Reference Command Center" MinWidth="960" MinHeight="640"
        WindowStartupLocation="CenterScreen" WindowState="Maximized"
        WindowStyle="None" ResizeMode="CanResize" Background="#050A10">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="36" ResizeBorderThickness="6" CornerRadius="0" GlassFrameThickness="0"/>
    </shell:WindowChrome.WindowChrome>
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="36"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Border Background="#050607" BorderBrush="#2B2F33" BorderThickness="0,0,0,1">
            <Grid Margin="12,0,0,0">
                <TextBlock VerticalAlignment="Center" Foreground="#DCE7E4" FontSize="12" Text="WPE Agent"/>
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <Button shell:WindowChrome.IsHitTestVisibleInChrome="True" Width="46" Background="Transparent" BorderThickness="0" Foreground="#AABBB7" Content="_" Click="Minimize_Click"/>
                    <Button shell:WindowChrome.IsHitTestVisibleInChrome="True" Width="46" Background="Transparent" BorderThickness="0" Foreground="#AABBB7" Content="□" Click="Maximize_Click"/>
                    <Button shell:WindowChrome.IsHitTestVisibleInChrome="True" Width="46" Background="Transparent" BorderThickness="0" Foreground="#DCE7E4" Content="×" Click="Close_Click"/>
                </StackPanel>
            </Grid>
        </Border>
        <wv2:WebView2 x:Name="ReferenceBrowser" Grid.Row="1"/>
    </Grid>
</Window>
