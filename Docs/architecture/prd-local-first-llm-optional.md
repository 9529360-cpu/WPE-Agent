# Retired: Local-First / LLM-Optional PRD

This document is retained only as historical context.

Current product truth:

- The trading brain is the built-in local deterministic technical brain.
- Hybrid, AI Research, Remote Brain, remote endpoint/key/token selection, and online LLM trading decisions are retired.
- Trading decisions must follow current market/candle evidence -> local decision -> deterministic plan -> risk -> execution/recovery.
- Historical trade data is for fills, PnL, execution audit, diagnostics, and risk/order/position facts. It must not become personality memory or change the next directional judgment through reflection.
- Old settings may be read for compatibility, but they normalize to the local deterministic runtime and are not exposed as switchable product capabilities.

Do not use this historical PRD to reintroduce remote Brain modes.
