# TODO

- [x] Close the 2026-07-28 runtime/UI gaps: hide historical handoffs without a fresh matching RunId lease, project automatic execution results, auto-start only after Testnet readiness, route Telegram configuration to the notification tab, and implement encrypted local Telegram subscriber intake, approval, scoped batch delivery, audit, runtime projection, and UI status.
- [ ] Move runtime snapshot refresh off the WPF UI thread into a background incremental snapshot pump with observable Telegram poll/dispatch health and backoff.
- [ ] Converge Chinese and non-Chinese routes onto one component tree, then complete the strategy evidence-to-execution explanation timeline.

## P0
- [ ] Restore a real GitHub Actions gate. Current private-repository pushes end in `startup_failure` before any job starts even though Actions is enabled; diagnose account/runner billing or platform eligibility, then require a successful remote build/test/publish-boundary run before treating CI as authoritative.
- [ ] Enable protected-main enforcement when the repository plan supports private-repository branch protection, or make an explicit repository-visibility/plan decision. The GitHub API currently returns HTTP 403 stating that Pro or a public repository is required.
- [x] Add focused Setup Bridge message tests.
- Keep the active safety suites in the release gate; add new collection-contract tests only together with the corresponding orders, equity, backtest, skill-call, or audit backend protocol.
- [x] Re-enable Agent settings resilience and Brain endpoint validation suites with the stage 2A configuration migration.
- [x] Verify order placement cannot bypass Risk Gate across all provider paths: current production order-path audit found no bypass evidence, and `TradingExecutionGatewayTests` adds a production-composition root guard around `ReliableOrderExecutor`; `43/43` passed, but this remains a source-wiring guard rather than a live exchange proof.
- [x] Audit logs and exception messages for secret redaction: audited and fixed redaction before `RealTimeDataPipeline` error-frame persistence and before both runners write `report.Cases`; `SensitiveDataRedactorTests` `6/6` and `AccessRunnerRedactionTests` `4/4` passed.
- [x] Connect the read-only trading-authorization projection to the Reference UI runtime snapshot: `DesktopRuntimeHost.BuildRuntimeJson()` refreshes and supplies `RuntimeAuthorization`; the desktop runtime-host regression tests pass. This is visibility only, not approval mutation or approved-order processing.

## P1
- [x] Establish a machine-enforced formal acceptance gate for all seven aggregate Agents. Require four Agent-specific evidence classes with exact environment, UTC validity, artifact SHA-256 and live provider identity; fail closed on missing, duplicate, stale, malformed, cross-Agent or wrong-environment evidence, and never auto-edit maturity authority.
- [x] Add the Market aggregate's credential-free target evidence runner. Require an official Binance Futures Testnet immediate read plus four samples over at least 15 minutes, canonical provenance, confirmed-candle and quality thresholds, bounded source age, more than one market state, atomic artifacts, and a read-only interface with no mutation authority.
- [x] Bind Macro revisions and the current-instrument Technical assessment as explicit canonical Research sources. Duplicate/invalid Macro facts and missing/conflicting/cross-identity Technical evidence fail closed before Strategy; facts-only evidence is not sufficient.
- [x] Give Backtest/Research validation its own persisted UTC validation time. Reject absent, non-UTC, future, and older-than-24-hour validation so a new cycle cannot relabel stale research as current.
- [x] Replace the generic Backtest capability admission with a versioned canonical validation fact. Bind exact symbol and strategy identity, validation freshness, coherent sample/trade counts, finite bounded metrics, lifecycle state and source hash; reject tampering, replay, cross-strategy sources and malformed metrics.
- [x] Replace the production orchestrator's uniform five-minute Research-source window with bounded source-specific lifetimes while keeping output and receipt time cycle-fresh. Preserve original timestamps and reject News over 7 days, Fundamental/Backtest over 24 hours, Macro over 62 days, and Market/technical evidence over the cycle window.
- [x] Separate current News authority from historical News research. Query persisted news by symbol and UTC backtest range, expose only news published before each closed-candle decision time, use a bounded 48-hour historical observation window, and remove same-bar return look-ahead from the simulator.
- [x] Make strategy lifecycle selection qualification-first and bounded. Start every seed in Draft, require Backtest then Shadow before Active, archive Degraded strategies, persist true lifecycle transitions, replenish two candidates per symbol/family from a six-variant deterministic ladder, and stop when the bounded search space is exhausted.
- [x] Derive bounded deterministic candidates from qualified Active parents only. Persist exact parent/version/generation, parameter and lineage hashes, enforce family parameter limits, and keep every child behind Draft, Backtest, Shadow, Risk Gate, and execution authorization.
- [x] Add a deterministic anti-overfit gate across four non-overlapping chronological regimes. Require at least three stable folds, bound the worst-fold loss and training/out-of-sample expectancy gap, persist the metrics, and include robustness in qualification scoring.
- [x] Harden the live Market Agent input with an independently hashed account source, fresh positive account truth, structurally valid non-duplicate positions, and per-market source status so one invalid market cannot relabel unrelated valid evidence.
- [x] Require the live Research Agent to consume an exact current-instrument validation that is structurally valid, approved, and promoted. Persist a bounded deterministic validation hash, reject missing/conflicting/wrong-instrument evidence, and keep invalid non-finite metrics out of canonical facts.
- [x] Make the live Strategy Agent independently require one fresh entry-ready assessment for the decision instrument, enforce directional agreement and valid entry/stop/take geometry for risk increase, and carry only the validated research strategy version into canonical facts.
- [x] Accept the bounded `strategy` component after binding the executable plan to the exact approved/promoted Research version and adding canonical identity, bounded confidence/tier, action, assessment and price-geometry rejection fixtures. Keep the Strategy aggregate partial pending autonomous lifecycle and live Testnet evidence.
- [x] Make the live Risk Agent validate an approved review as a coherent fact set: positive quantity/risk/exposure for risk increase, no simultaneous blocking reasons, a valid non-duplicated check set with deterministic hash, and a non-blocked risk level.
- [x] Validate automatic execution timelines as contiguous, causally linked, monotonic UTC event chains before Execution or Recovery can succeed. Let the Audit Agent successfully record valid failed/unknown outcomes as exceptions without misreporting the trade as successful; structurally invalid timelines keep Audit blocked.
- [x] Require the canonical seven-Agent Risk input to consume fresh, tamper-evident Position quantity, protection, and external-isolation reports. Missing, replayed, malformed, conflicting, incomplete, stale, or isolated evidence blocks risk increase while reduce-only recovery remains available.
- [x] Bind bounded canonical News evidence into the live seven-Agent Research input. Validate publisher/HTTPS-host identity, timestamps, duplicate identity and finite structured fields; keep article text out of canonical audit and fail closed on spoofed, malformed, duplicate, or wholly unavailable News evidence.
- [x] Accept the bounded `news` component after canonical admission and adversarial fixtures passed. Keep Research partial and do not claim live source availability, article truth, causal interpretation, or provider certification.
- [x] Add the first production model-off seven-Agent orchestration boundary for canonical Market/Research/Strategy/Risk inputs, deterministic ordered handoffs, zero-mutation Execution/Recovery, and fail-closed Audit coverage. Connecting live runtime producers and Testnet mutation remains a later gated batch.
- [x] Connect existing live Evidence/Research/Strategy/Risk runtime truth to the production seven-Agent orchestrator as an observation-only shadow before order authorization. Promotion from shadow evidence to a mandatory execution gate remains a separate acceptance decision.
- [x] Project persisted automatic Testnet queue transitions and append-only events into immutable canonical Execution, Recovery, and Audit Agent observations. Exchange-order detail correlation and promotion of these observations into a mandatory risk-increase gate remain separate work.
- [x] Correlate successful automatic execution observations with same-cycle persisted order intents and fail closed on missing, extra, malformed, unconfirmed, or conflicting order evidence. Direct read-only exchange reconciliation and mandatory risk-increase gate promotion remain separate work.
- [x] Promote the persisted seven-Agent model-off cycle to a mandatory pre-authorization gate for non-reduce-only risk increase while leaving reduce-only recovery available. Direct read-only exchange reconciliation remains separate work.
- [x] Add versioned, canonical, read-only exchange order evidence after automatic execution/reconciliation and require independently validated `FILLED` evidence before the Execution Agent reports success. No mutation method is exposed through the evidence reader.
- [x] Add bounded, idempotent startup backfill for missing Execution/Recovery/Audit observations, with persisted global event-cursor paging, failure-safe cursor advancement, a bounded unresolved-evidence retry path, append-only Unknown-to-Confirmed upgrades, and state/timeline consistency checks.
- [x] Expose the persisted strategy registry and lifecycle events to the Web UI with explicit unavailable, stale, error, and empty states.
- [x] Add historical orders, equity, backtest, skill-call, and audit collection protocols with bounded signed cursors, stale/error withholding, SQLite read-only projection, runtime bridge integration, and focused tests.
- [x] Add the first deterministic Macro research component: a versioned observed-fact contract requiring canonical Macro source metadata, UTC observation/release times, geography, frequency, unit, and numeric value. Live official-source ingestion and revision history remain open, so the full Macro capability is not promoted.
- [x] Add the first keyless official Macro adapter for allowlisted BLS public series. It pins the official HTTPS endpoint, performs read-only POST, bounds responses, hashes raw evidence, labels fetch time as first-observed rather than formal release time, and fails closed on malformed/unavailable data. Persistent revision history and release-calendar correlation remain open.
- [x] Start official BLS Macro collection with the production Agent, persist append-only restart-safe revisions, and compose the latest valid revision per indicator into every live canonical Research input without introducing model inference or trading conclusions. Official release-calendar correlation remains open.
- [x] Add strict official BLS release-calendar correlation with pinned read-only transport, bounded ICS parsing, UTC conversion, unique event matching, raw-calendar hashing, append-only provenance upgrades, and health degradation unless both CPI and Employment Situation observations receive formal release evidence. Live target-machine calendar availability remains required before live Macro certification.
- [x] Accept the bounded observation-only `macro` component after rejecting future/out-of-window/duplicate observations and proving offline refresh preserves the last valid append-only fact. Keep Research partial; do not claim forecasts, causal trading conclusions, broad macro coverage, or target-machine live availability.
- [x] Replace the unsupported Fundamental placeholder with a bounded model-off crypto instrument fact contract. Binance Futures Testnet now reads official public `exchangeInfo`, validates exact symbol identity, active perpetual status, base/quote/margin assets, onboard time, freshness and canonical hashes, and requires current target-instrument evidence before Research can pass. This first stage was `partial`; the later exact-identity batch accepted only the venue-instrument scope.
- [x] Accept the bounded `fundamental` venue-instrument component after locking it to exact Binance Futures Testnet provider/environment/canonical identity and rejecting aliases, case changes, cross-symbol mappings and self-consistent unsupported providers. Keep Research partial and do not claim protocol, issuer, tokenomics, on-chain, valuation or investment analysis.
- [x] Implement the bounded Teacher capability as a deterministic once-daily market brief sourced only from eligible canonical Research, with fixed financial-term explanations and explicit `MarketBrief` notification opt-in. Interactive teaching, quizzes, scoring, model-written commentary, and trading authority are outside the accepted scope.
- [x] Implement Teacher V2 Phase A: persona 2.0, 08:00/14:00/20:00 Asia/Shanghai planning, exact seven-Agent canonical references, deterministic Chinese Local Only lessons, typed report blocks, append-only lesson/recommendation/correction/delivery/memory storage, recommendation expiry, numeric provenance guard, and non-blocking production archive wiring with no execution authority.
- [x] Accept the bounded local-first crypto Teacher V2 scope. Candidate `3.6.0.0` SHA-256 `e1fb2332befc093f04e6dd84b03be76238ef5a302fe785e8c4fee7260a35308c` passed `1862/1862` Release tests and four canonical public samples over 15.02 minutes with no credential or mutation. Keep equities, broad news/macro/cross-asset adapters and commercial redistribution outside this acceptance.
- [x] Add deterministic Teacher crypto event lessons and production monitoring outside trading authority. Bind previous/current public hashes, separate fact/hypothesis/alternative/unknown blocks, suppress duplicate event fingerprints, and require independent opt-in kinds for morning, afternoon, evening, event, recommendation and correction notifications.
- [x] Add point-in-time Teacher recommendation evaluation. Bind the original public entry hash, reject evidence first retrieved after the simulated decision time, require instrument and BTC benchmark endpoints, calculate deterministic absolute/relative return and favorable/adverse excursion, and persist outcomes append-only without promoting lucky or invalidated processes.
- [x] Expose explicit `MarketBrief` consent through secure WPF notification settings, persist it through the existing configuration service, and show the allowed event set read-only in Web runtime settings.
- [x] Consolidate or quarantine legacy Binance-direct services. Deleted execution types are absent from the production assembly, residual reference-only desktop modules are excluded from compilation, and regression tests prevent production composition from registering the old Binance stack.
- [x] Make confirmed-close post-trade accounting idempotent by client order identity, reject conflicting replays, exclude partial fills from realized PnL, calculate entry cost from the deterministic moving-weighted execution ledger, and persist a bounded review fact into local long-term memory. Exchange-reported fee attribution and broader causal strategy attribution remain open.
- [x] Attribute post-trade reviews to the exact persisted automatic execution artifact when correlation resolves to one strategy ID/version. Legacy calls remain explicitly version-only and conflicting correlations remain unattributed; causal performance attribution remains open.
- [x] Add append-only exchange-reported fee evidence for Binance Futures Testnet through the signed read-only user-trades endpoint. Post-trade review uses reported fees only when every contributing entry and the close have fresh, canonical, quantity-complete USDT evidence; missing, invalid, mixed-asset, unsupported, stale, or errored evidence keeps the explicit static estimate. Reported fees for other providers and funding attribution remain unsupported.
- [x] Attribute provider-neutral execution slippage from the persisted intent expected price to confirmed fill averages. Post-trade review 1.3 records signed entry, exit and total amounts (positive adverse, negative favorable), proportionally preserves remaining-entry attribution across reductions, rejects replayed close identities with changed expectations, and never subtracts the explanatory amount from realized PnL a second time. Legacy rows remain explicitly `unavailable`; funding was completed in the subsequent 1.4 batch.
- [x] Add append-only Binance Futures Testnet funding-income evidence through the signed read-only income endpoint. Post-trade review 1.4 uses signed USDT funding only for a completely closed, single-side, unreduced position epoch covered by a successful bounded observation window; empty successful windows prove zero, while partial, hedge-ambiguous, legacy, stale, malformed, unsupported, or errored evidence remains explicitly unavailable. Other providers remain unsupported.
- [x] Accept the bounded `review-post-trade` component after fee, funding, slippage, replay, strategy-identity, and legacy-migration coverage passed. Keep the Audit aggregate partial and add an exact runtime-registry versus machine-authority consistency gate so maturity facts cannot silently drift again.
- [x] Add a versioned canonical Position reconciliation gate that compares the decimal local execution ledger with fresh provider positions, persists tamper-evident evidence, blocks new risk on every unknown/conflicting state, and leaves reduce-only recovery available. Protection-order reconciliation and explicit external-position isolation are now completed as separate read-only gates.
- [x] Isolate external-only and exchange-excess position quantities as versioned canonical read-only facts. Persist them append-only, block new risk on isolated/stale/invalid evidence, never mutate or adopt the external position, and leave reduce-only recovery available.
- [x] Accept the bounded Position component after closing the recovery TOCTOU, unclassified-trigger, and database mutability gaps. Any successful recovery mutation invalidates current-cycle risk-increase authority; generic duplicate protection orders remain incomplete; all three Position audit tables reject update/delete. Execution and Recovery aggregates remain partial pending live Testnet certification.
- [x] Accept the bounded Orchestrator/control component after making the seven-output/six-handoff production cycle atomic, idempotent, append-only, and conflict-failing. Keep all seven aggregate Agents partial and make no live Testnet mutation claim.
- [x] Stop unknown or malformed realtime market events from refreshing freshness, require complete price and bid/ask evidence for hub health and enrichment, and preserve REST evidence when the stream snapshot is incomplete. Provider raw-source and REST/WS consistency evidence remain open before Market Data acceptance.
- [x] Build REST market facts only from valid unique fully closed candles and use the confirmed close time as Market `CollectedAt` across Binance and shared provider adapters. Persisted raw-response provenance and cross-source identity remain open before Market Data acceptance.
- [x] Accept bounded Market Data after binding each final market fact to Testnet provider identity, symbol, source time, confirmed-candle bounds and canonical SHA-256, with independent Market Agent verification. Keep the Market aggregate partial; do not claim raw HTTP retention or live provider certification.
- [x] Accept bounded Technical after adding a versioned fact schema, source hash/time/range validation, canonical Market binding and deterministic weighted aggregation. Keep Research partial and do not claim independent or live certification of provider multi-timeframe calculations.
- [x] Replace the position-count protection heuristic with versioned read-only SL/TP reconciliation and tamper-evident persistence. Missing, orphaned, stale, or invalid protection blocks new risk without attempting an exchange mutation. Bybit position-level TP/SL is now observed read-only through `/v5/position/list`, and synthetic evidence cannot enter the ordinary order-cancellation endpoint.

## P2
- Add a portable Windows release package that keeps immutable application files separate from `%LOCALAPPDATA%\WPE Agent` user data, includes a signed/hash manifest, and supports side-by-side last-known-good rollback without copying secrets or runtime databases into the package.
- [x] Add compact context/input-token/output-token/cache/latency/cost metrics for Brain calls to local SQLite and the runtime snapshot without exposing raw prompts or responses.
- [x] Add bounded, symbol-scoped, balanced working/episodic/long-term memory retrieval using existing SQLite and feed its short versioned projection into production planning without changing deterministic safety authority.
- [x] Token milestone order completed for the July 21, 2026 batch: landed `two-level planner prompt slimming`, `structured short memory` planner-history compression, and the `runtime/UI` governance baseline.
- [x] Replace `RecentOutcomesAsync` free-text planner history with bounded `structured short memory` that reuses existing SQLite memory tiers without weakening fail-closed trading context.
- [x] First `structured short memory` batch is contract-only: added the read-side `StructuredOutcomeMemory[]` projection plus narrow store/persistence/degraded-path tests before changing `AutoTradingAgent` or deleting the old text aggregation path.
- [x] Prompt-compression follow-up from the July 21, 2026 audit: `Markets` brief now uses a bounded priority subset, slim-mode `marketAssessments.Signals` now keeps only directional essentials, and slim-mode news no longer carries `BodySummary`.
- [x] Expose the runtime/UI governance baseline clearly enough to track token/cost/remote-usage regression without re-reading prompt internals.
- [x] Switch `AutoTradingAgent` from legacy `PreviousOutcomes` string consumption to the structured memory contract directly; emit a bounded versioned short-key projection only when a remote planner is selected, while Local Only continues to operate without a model.
