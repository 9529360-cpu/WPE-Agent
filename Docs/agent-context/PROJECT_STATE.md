# Project State

- Current source: `WPE-` worktree.
- Authoritative product/assembly version: the application project's `<Version>` (`3.6.0` at this milestone). Runtime, plugin compatibility, release reports and packaging read the generated assembly/project version.
- `WPE-Agent-3.8.2-P0-Truth` is a historical validation label, not an assembly version or a distributable release version.
- Mutable runtime data lives under `%LOCALAPPDATA%\WPE Agent\`; the application installation directory is treated as read-only.
- Desktop entry: `币安量化机器人.exe --reference-ui`.
- Setup Bridge opens the existing secure WPF `SetupWindow` from `/settings`.
- Runtime environment now comes from `SystemState.Mode`; stale runtime data is exposed to Web UI.
- Access, architecture, strategy lifecycle, frontend, .NET build, publish, and launch checks pass.
- The isolated `WPE.Tests` project is discovery-guarded by `eng/test.ps1`. Current safety coverage includes active configuration, Risk Gate, execution mutation, trading gateway, auto-trading authorization, unknown-order recovery, secret redaction, provider read-only contracts, and bounded historical collection protocols.
- The bounded `review-post-trade` capability is accepted for deterministic confirmed-close accounting: durable close identity, decimal moving-weighted entry cost, explicit fee provenance, signed funding when unambiguous, explanatory slippage without double charging, strategy identity, and append-only long-term review memory. This does not accept the full Audit Agent or claim broad causal strategy performance.
- The bounded `position` capability is accepted for deterministic local/provider quantity reconciliation, explicit protection proof, external-position isolation, append-only audits, and same-cycle authority invalidation after recovery mutation. This does not accept the full Execution or Recovery Agent and is not live Testnet provider certification.
- The bounded `orchestrator` capability is accepted for the deterministic seven-role canonical chain, mandatory fail-closed pre-authorization gating, and atomic append-only persistence of all seven outputs plus six handoffs. It does not accept any of the seven aggregate Agents or certify live Testnet mutation.
- The bounded `market-data` capability is accepted for confirmed-candle facts, source-time freshness, Testnet provider-bound canonical provenance, and complete realtime enrichment gates. It does not accept the full Market Agent, retain raw HTTP responses, or certify live providers.
- The bounded `technical` capability is accepted for versioned technical facts and deterministic weighted signal aggregation over canonical Market evidence. It does not accept the Research aggregate or independently certify provider multi-timeframe calculations.
- Migration stage 2A hardened configuration only: corrupt Agent settings fail closed with safe diagnostics, saves are atomic, DPAPI failures remain explicit, environment credential slots do not fall back, Setup synchronizes its Testnet slot and active profile through the store, Binance Testnet REST/WS origins are constrained to official endpoints, and Brain endpoints enforce provider-specific host rules. The two configuration suites are enabled; 33 tests pass.
- Repository maintenance now aligns the README, explicit CI commands, WPE-specific sensitive-file exclusions, and publish-boundary checks with current product truth. Technical Research additionally rejects noncanonical and cross-symbol Market bindings. Local Release validation passes `1732/1732` tests with zero build warnings/errors and a clean publish-boundary scan.
- The bounded `news` capability is accepted for deterministic canonical evidence admission into Research. Spoofed hosts, stale/non-UTC items, duplicate identities, invalid structured values, and complete source unavailability fail closed; article text stays outside canonical Agent audit. Live source availability and narrative truth are not certified, and the Research aggregate remains partial.
- The bounded `strategy` capability is accepted for deterministic executable-plan validation tied to the exact current-instrument assessment and approved/promoted Research strategy version. Invalid action, identity, confidence, tier, direction, or price geometry fails closed. Autonomous strategy discovery, full lifecycle acceptance, and live Testnet performance remain open; the Strategy aggregate remains partial.
- The bounded `macro` capability is accepted for observation-only ingestion of two official BLS series, append-only revision/provenance history, strict release-calendar correlation, and degraded last-known-fact retention. It rejects future, out-of-range, duplicate and malformed observations. It does not forecast or certify target-machine BLS availability; the Research aggregate remains partial.
- The bounded `fundamental` capability is accepted for exact Binance Futures Testnet venue-instrument metadata from official `exchangeInfo`. Provider/environment/symbol identity, active perpetual status, asset coherence, time and hashes are deterministic and fail closed. Protocol health, issuer quality, tokenomics, on-chain evidence, valuation and investment conclusions remain outside scope; the Research aggregate remains partial.

Known gaps: GitHub Actions is enabled but recent pushes terminate as `startup_failure` before creating a job, so remote CI is not accepted despite passing local CI-equivalent validation. Branch protection is unavailable for this private repository under the current GitHub plan. Target-machine live availability evidence for the official BLS release calendar, protocol/tokenomics/on-chain Fundamental evidence beyond verified Binance Testnet instrument metadata, broader causal post-trade strategy attribution, and fee/funding/fundamental certification for providers other than Binance Futures Testnet also remain open. Binance Futures Testnet funding attribution is accepted only for a complete, unambiguous single-side position epoch covered by a successful signed read-only income window; partial reductions, any opposite-side overlap, incomplete/error evidence, legacy fills without exchange timestamps, and non-USDT income remain explicitly unavailable. Provider-neutral intent-versus-fill slippage attribution is persisted, but venue-specific latency and market-impact decomposition remain open. Legacy Binance-direct execution services are excluded from production compilation and guarded by regression tests.
