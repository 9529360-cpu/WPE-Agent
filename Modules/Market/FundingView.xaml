<UserControl x:Class="币安量化机器人.Modules.Market.FundingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sp="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF">
    <UserControl.Resources>
        <Style x:Key="RightAlignCell" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="TextAlignment" Value="Right"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#F5F7FA" Padding="12">
            <DockPanel LastChildFill="False">
                <StackPanel Orientation="Vertical" DockPanel.Dock="Left">
                    <TextBlock Text="资金费率监控" FontSize="16" FontWeight="Bold"/>
                    <TextBlock Text="实时拉取 Binance 永续合约资金费率与标记价数据" Foreground="#6B7280" FontSize="12"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBox x:Name="SearchBox"
                             Width="220" Height="28" Margin="0,0,12,0"
                             VerticalContentAlignment="Center"
                             TextChanged="SearchBox_TextChanged"/>
                    <Button Content="刷新数据" Height="28" MinWidth="88" Click="Refresh_Click"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Main -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="380"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="#E1E6EB" BorderThickness="1" CornerRadius="6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#FAFBFC" Padding="8,4">
                        <UniformGrid Columns="5">
                            <TextBlock Text="交易对" FontWeight="Bold"/>
                            <TextBlock Text="最新资金率" FontWeight="Bold" TextAlignment="Right"/>
                            <TextBlock Text="预测资金率" FontWeight="Bold" TextAlignment="Right"/>
                            <TextBlock Text="7日均值" FontWeight="Bold" TextAlignment="Right"/>
                            <TextBlock Text="未平仓量(USDT)" FontWeight="Bold" TextAlignment="Right"/>
                        </UniformGrid>
                    </Border>

                    <DataGrid x:Name="FundingGrid"
                              Grid.Row="1" Margin="0"
                              AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                              HeadersVisibility="None" SelectionMode="Single"
                              SelectionChanged="FundingGrid_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding Pair}" Width="*"/>
                            <DataGridTextColumn Binding="{Binding LastFundingRate, StringFormat=F5}" Width="130" ElementStyle="{StaticResource RightAlignCell}"/>
                            <DataGridTextColumn Binding="{Binding PredictedFundingRate, StringFormat=F5}" Width="130" ElementStyle="{StaticResource RightAlignCell}"/>
                            <DataGridTextColumn Binding="{Binding Avg7dFundingRate, StringFormat=F5}" Width="130" ElementStyle="{StaticResource RightAlignCell}"/>
                            <DataGridTextColumn Binding="{Binding OpenInterest, StringFormat=N0}" Width="150" ElementStyle="{StaticResource RightAlignCell}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Grid.Column="2" BorderBrush="#E1E6EB" BorderThickness="1" CornerRadius="6" Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="DetailTitle" Text="请选择左侧交易对" FontSize="15" FontWeight="Bold"/>
                    <StackPanel Grid.Row="1" Margin="0,6,0,0">
                        <TextBlock x:Name="NextFundingText" Foreground="#374151"/>
                        <TextBlock x:Name="BasisText" Foreground="#6B7280" FontSize="12" Margin="0,4,0,0"/>
                    </StackPanel>

                    <GroupBox Header="资金率走势" Grid.Row="2" Margin="0,12,0,0">
                        <sp:WpfPlot x:Name="FundingPlot" Height="180"/>
                    </GroupBox>

                    <StackPanel Grid.Row="3" Margin="0,12,0,0">
                        <TextBlock Text="核心指标" FontWeight="Bold" Margin="0,0,0,6"/>
                        <UniformGrid Columns="2" Rows="4" Margin="0,0,0,6">
                            <TextBlock Text="合约类型"/>
                            <TextBlock x:Name="ContractText" TextAlignment="Right"/>
                            <TextBlock Text="基差(%)"/>
                            <TextBlock x:Name="BasisPctText" TextAlignment="Right"/>
                            <TextBlock Text="24h 指数价"/>
                            <TextBlock x:Name="IndexPriceText" TextAlignment="Right"/>
                            <TextBlock Text="24h 标记价"/>
                            <TextBlock x:Name="MarkPriceText" TextAlignment="Right"/>
                        </UniformGrid>
                        <TextBlock x:Name="StatusDetailText" Foreground="#6B7280" TextWrapping="Wrap"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <StatusBar Grid.Row="2" Background="#F5F7FA">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="状态：等待加载"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
