'use client'
import {ShieldAlert} from 'lucide-react';import {Panel,PanelBody,PanelHeader} from '@/components/ui/panel';import {RuntimeMetric,RuntimeUnavailable} from '@/components/runtime-state';import {useWpeRuntime} from '@/components/runtime-bridge';import {useI18n} from '@/lib/i18n/context'
export function RiskSummary(){const r=useWpeRuntime(),{t}=useI18n();return <Panel><PanelHeader icon={<ShieldAlert className="size-4"/>} title={t('dashboard.riskSummary')}/>{r.runtimeFresh?<PanelBody className="grid gap-3 sm:grid-cols-2"><RuntimeMetric label={t('dashboard.riskGate')} value={r.riskApprovalStatus}/><RuntimeMetric label={t('dashboard.riskLoad')} value={r.riskLoad}/><RuntimeMetric label={t('dashboard.maxDrawdown')} value={r.maxDrawdown}/><RuntimeMetric label={t('dashboard.riskSummary')} value={r.riskSummary}/></PanelBody>:<div className="p-4"><RuntimeUnavailable stale={Boolean(r.lastUpdated)} subject={t('risk.metrics')}/></div>}</Panel>}
