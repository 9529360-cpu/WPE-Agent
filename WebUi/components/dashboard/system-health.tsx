'use client'
import {ServerCog} from 'lucide-react';import {Panel,PanelBody,PanelHeader} from '@/components/ui/panel';import {RuntimeMetric,RuntimeUnavailable} from '@/components/runtime-state';import {useWpeRuntime} from '@/components/runtime-bridge';import {useI18n} from '@/lib/i18n/context'
export function SystemHealth(){const r=useWpeRuntime(),{t}=useI18n();return <Panel><PanelHeader icon={<ServerCog className="size-4"/>} title={t('dashboard.systemHealth')}/>{r.runtimeFresh?<PanelBody className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><RuntimeMetric label="Agent" value={r.status}/><RuntimeMetric label={t('dashboard.exchange')} value={r.exchangeConnected?t('common.healthy'):t('common.notConnected')}/><RuntimeMetric label={t('dashboard.brain')} value={r.brainConnected?t('common.healthy'):t('common.notConnected')}/><RuntimeMetric label={t('dashboard.risk')} value={r.riskReady?t('common.ready'):t('common.notReady')}/></PanelBody>:<div className="p-4"><RuntimeUnavailable stale={Boolean(r.lastUpdated)} subject={t('dashboard.systemHealth')}/></div>}</Panel>}
