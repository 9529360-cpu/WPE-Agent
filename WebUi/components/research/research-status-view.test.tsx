import assert from 'node:assert/strict';import test from 'node:test';import {renderToStaticMarkup} from 'react-dom/server';import {ResearchStatusView,type ResearchProjection} from './research-status-view'
const render=(projection:ResearchProjection)=>renderToStaticMarkup(<ResearchStatusView projection={projection}/>)
const item={researchId:'a',strategyId:'host-strategy',instrumentId:'host-instrument',assetClass:'Crypto',status:'Available' as const,lifecycle:'Draft' as const,passed:false,reasonCodes:['host.reason'],metrics:{observations:200,trades:10,totalReturn:.1,outOfSampleReturn:.02,maximumDrawdown:.05,sharpe:1.1,walkForwardScore:.7,monteCarloLossProbability:.2},artifactHash:'a'.repeat(64),datasetHash:'b'.repeat(64),parameterHash:'c'.repeat(64),codeVersion:'1',strategyVersion:'2',seed:7,outOfSampleStartsAtUtc:'2026-01-01T00:00:00Z',outOfSampleEndsAtUtc:'2026-02-01T00:00:00Z',oosPurgeObservations:5,oosEmbargoObservations:5,alignment:{required:false,venueCount:0,alignmentPointCount:0,synchronizationWindowSeconds:null,maximumTimestampDeviationSeconds:null,forwardFillAllowed:false},multipleTesting:{trialCount:2,correctionMethod:'Bonferroni',nominalAlpha:.05,correctedSignificanceThreshold:.025,holdoutUntouched:true,holdoutUsedForSelection:false,holdoutEvaluationCount:1},evaluatedAtUtc:'2026-02-02T00:00:00Z'}
test('fails closed for unsupported stale and error',()=>{for(const state of ['unsupported','stale','error'] as const){const html=render({state,items:[{...item,strategyId:'MUST-NOT-RENDER'}]});assert.doesNotMatch(html,/MUST-NOT-RENDER/)}})
test('renders the real available empty state',()=>assert.match(render({state:'available',items:[]}),/No research artifacts/))
test('renders only host artifact summaries',()=>{const html=render({state:'available',items:[item]});assert.match(html,/host-strategy/);assert.match(html,/host.reason/);assert.match(html,/Bonferroni/);assert.doesNotMatch(html,/profit guaranteed|execute|submit order|trialPValues/i)})
