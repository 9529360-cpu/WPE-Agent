'use client'
import {StatusDot} from '@/components/ui/status-badge'
import {useWpeRuntime} from '@/components/runtime-bridge'
import {useI18n} from '@/lib/i18n/context'
export function StatusBar(){const runtime=useWpeRuntime(),{t,formatDate}=useI18n(),fresh=runtime.runtimeFresh===true,running=runtime.agentIsRunning===true;return <footer className="flex min-h-9 min-w-0 flex-wrap items-center gap-x-3 gap-y-1 overflow-hidden border-t border-border bg-sidebar px-4 py-1 font-mono text-[11px] text-muted-foreground"><span className="flex shrink-0 items-center gap-1.5"><StatusDot token={fresh?'success':'warning'} pulse={fresh}/>{t(fresh?'common.runtimeOnline':'common.runtimeOffline')}</span><span className="truncate">{runtime.runtimeProviderId??'provider unavailable'} · {runtime.environment??'environment unavailable'}</span><span className="hidden truncate lg:inline">source {runtime.sourceTimestampUtc?formatDate(runtime.sourceTimestampUtc):'unavailable'} · snapshot {runtime.snapshotTimestampUtc?formatDate(runtime.snapshotTimestampUtc):'unavailable'}</span><span className={fresh?'text-success':'text-warning'}>{fresh?`fresh ${Math.round(runtime.runtimeAgeSeconds??0)}s`:runtime.runtimeDiagnosticReason??'runtime unavailable'}</span><span className="ml-auto flex shrink-0 items-center gap-1.5"><StatusDot token={running?'success':'muted'} pulse={fresh&&running}/>{t(running?'agents.running':'agents.stopped')}</span></footer>}
